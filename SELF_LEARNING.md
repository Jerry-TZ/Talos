# Talos 自学习设计 — 怎么让一个 agent "学会"东西

> 这是 Step 4 的说明。目标:教会你自学习是**怎么回事、怎么实现**。先抄 Hermes 的
> 作业,再提炼成通用机制,最后落到 Talos 上(你会发现几乎不用加新代码)。

---

## 0. 先破除最大的误解:自学习 ≠ 训练模型

模型是**冻结的、租来的**,你不碰它的权重。所谓 "agent 会学习",不是模型变聪明,而是:

> **agent 把学到的东西写成文字笔记,下次自己读回来。**

- 没有微调、没有 ML、没有 GPU。就是**写 markdown、下次读回来**。
- 便宜、透明、可 `git diff`、可 fork。
- 类比:一个记性为零但很聪明的助理 —— 你让他每次把心得记在便签上,下次先翻便签。
  他"越来越懂你",靠的是便签越攒越多,不是脑子变了。

---

## 1. 先看 Hermes 怎么做的(抄作业)

Hermes(Nous Research)是目前"自我进化"做得最实的。和学习相关的是它的
**Memory + Skills + Self-Improvement** 三块:

### 两层知识,分工明确

| | 存什么 | 什么时候在上下文 | 存哪 |
|---|--------|----------------|------|
| **Memory**(`MEMORY.md`/`USER.md`) | 关于你/项目/环境的**小而持久的事实** | **始终常驻** | 跨会话增长,可全文检索 |
| **Skills**(`SKILL.md`) | **较长的操作流程/做法** | **只在相关时加载** | `~/.hermes/skills/`,agentskills.io 格式 |

一句话:**Memory 存事实(常驻),Skills 存做法(按需)。** SKILL.md 的 frontmatter
声明"**何时适用**",正文写"**怎么做**"。

### 什么时候写一条 skill(触发条件 —— 关键)

- 完成一个**复杂任务(≥5 次工具调用)**后,自动写一条;
- 或**同类任务成功跑了 3~4 次**后,把套路固化成 skill;
- 外加一个**每轮结束的后台"自我复盘"**也会写。
- 增/改/删自己的 skill 通过一个 `skill_manage` 工具。

### 自我改进 & 省钱检索

- skill 在**反复使用中被 refine** —— "这次又多懂一点" → 更新那条。把"我搞定过一次"
  变成"下次它主动想起来"。
- skill **只有被调用时才耗 token**;每个任务 agent 自己判断哪些相关,**只加载相关的**。
  (= 渐进式披露:平时只看"何时适用"那一行,真要用才读正文。)

### 安全阀

可开**写入审批闸**:skill 改动要人批准才落盘(默认自由写)。—— 记住这点,后面 Talos 白捡。

---

## 2. 提炼成通用机制:自学习就是 4 步循环

不管哪个 agent,自学习都是这四步:

```
  ①做 ──▶ ②复盘 ──▶ (写 skill / 记 memory) 
   ▲                                    │
   └──────── ③取用(下次开工先读) ◀───────┘
                     ④整理(定期合并去重)
```

1. **做 (Do)**:正常跑任务。
2. **复盘 (Reflect)**:任务后(或被你纠正后、或超过 N 步后)问模型一句:
   *"这次有没有可复用的**做法**、或值得记的**事实**?有就写成 skill / 记进 memory。"*
   —— 一次小小的额外模型调用,让它去**写文件**。
3. **取用 (Retrieve)**:开工时,加载 memory(全部,它小)+ 所有 skill 的**一行简介**
   (便宜);真要用某条,才读它**正文**。
4. **整理 (Consolidate)**:定期合并重复、删掉没人用的、保持质量。
   **—— 这是最难的一步,skill 库烂就烂在这。**

**为什么"简洁"和"会学习"不打架:** 引擎一行不加,长大的是**文件**。上下文成本 =
memory + N 行简介,**不是** N 篇正文。这就是它俩能共存的秘密。

---

## 3. 在 Talos 里怎么落地(几乎不加新代码)

关键洞察:**Step 4 主要靠复用 Step 1/2 已经有的东西。**

- **存哪**:`talos/memory.md`(事实)+ `talos/skills/<名字>.md`(做法)。
- **取用**:Talos **已经有 `read_file` 了**。所以把 memory.md 内容 + 各 skill 的
  `名字: 简介` 塞进 system prompt(便宜),模型要用哪条,自己 `read_file` 读正文。
  **零新工具。**
- **复盘**:任务结束后,用一个特殊 system prompt 再跑一次 `agent_turn`:
  *"回顾刚才,值得存的做法/事实就用 write_file/edit_file 写进 skills/ 或 memory.md,
  没有就什么都不做。"* —— 复用现成的 `write_file`/`edit_file`,而且**自动走 Step 2
  权限门**(写 skill 也要你点允许 —— 这就是白捡 Hermes 的"写入审批闸"!)。
- **整理**:一个 `/consolidate` 命令,让模型合并/精简 skills。还是复用工具。

所以 Step 4 的新东西其实只有:**几个提示词 + 两处文件读写 + 一段加载逻辑**。
本质就是 —— **给 Talos 装一个"给自己记笔记"的回路。**

### 长这样(具体例子)

一条 `skills/run-tests.md`:
```markdown
---
name: run-tests
description: 当用户想跑测试或验证改动是否有效时使用
---
1. 先探 `run_bash("pytest --version")`
2. 有 pytest 就 `pytest -q`;没有就找 test_*.py 手动跑
3. 失败时,把最短的那段报错回给用户,别贴一大坨
```

`memory.md`:
```markdown
- 用户偏好:中文回答,代码注释也中文。
- 本项目:Python + Anthropic SDK,模型 claude-opus-4-8。
- 教训:改文件前先 read_file 看清上下文,别凭空 write。
```

---

## 4. 真正难的 20%(说实话)

管道不难,难在**让存下来的是精华、不是垃圾**:

- **什么值得存?** 一次性的别存(库污染)。用触发条件卡:≥N 步、被纠正、或同类重复。
- **写得小而准**,而且以后**检索得到**(简介写好;skill 破百了再上 embedding 索引)。
- **定期清理**,别让库烂掉、检索变差 —— 大多数"自我进化 agent"就死在这。
- **安全**:抄 Hermes 的写入审批闸 —— 我们天然有(Step 2 权限门),skill 写盘也要你批。

---

## 5. 最小实现清单(下一步真写代码时)

1. `retrieve()`:启动/每轮开头,读 `memory.md` + 收集 `skills/*.md` 的 frontmatter 简介
   → 注入 system prompt(**只注入简介,不注入正文**)。
2. `reflect()`:任务后一次特殊 `agent_turn`,产出/更新 skill 或 memory(**走权限门**)。
3. `/consolidate` 命令:合并去重。
4. `--selfcheck` 补:skill 的 frontmatter 解析、`retrieve()` 确认只注入简介不注入正文。

> 一句话记住:**学习 = 写便签 + 下次先翻便签。** 剩下的全是"什么值得写、怎么翻得到"。
