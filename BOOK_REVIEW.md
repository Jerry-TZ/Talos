# 对着《深入理解 AI Agent》读 Talos

拿李博杰《深入理解 AI Agent:设计原理与工程实践》([bojieli/ai-agent-book](https://github.com/bojieli/ai-agent-book))
逐条比对 Talos 的实现,列出该改的、不该抄的、以及书替我们说出来的话。

书的主线公式是 `Agent = LLM + 上下文 + 工具`。跟 Talos 同一个战场的是四章:
ch2 上下文工程、ch3 用户记忆、ch4 工具与权限、ch5 Coding Agent、ch6 评估、ch8 持续进化。
ch7(后训练)、ch9(多模态)、ch10(多智能体)跟这个项目无关。

---

## 一、书把 Talos 三次翻车逐字预测了

`FINDINGS.md` 里那张翻车表,有三行能在书里找到对应的原文。这不是巧合 ——
是这些坑本来就有人踩过、写下来了,而我们靠十六个任务重新踩了一遍。

### 1. 变形命令绕过黑名单(第六次翻车)

> **ch4:192** —— "命令执行维护一份禁止命令的黑名单...需要注意的是,**黑名单只是最基础的
> 防护层,不应作为唯一手段 —— 攻击者可以通过变形命令绕过简单的字符串匹配**。更健壮的
> 方案是结合**语义解析**,理解命令的实际意图而非仅匹配表面形式"

Talos 的实况:`del skills\x.md` 被拒 → `cmd /c del skills\x.md` 六个技能文件无声删除。
`_DESTRUCTIVE` 至今被绕过三次,每次都是把动词换个壳。

### 2. 拒绝之后模型学不到东西(任务 15)

> **ch4:204** —— "审批失败后**不应简单重试,而应将拒绝理由作为工具调用结果加入 Agent
> 的轨迹**。从提议模型的视角看,审批拒绝就像一次工具调用失败,返回了错误信息和修正建议"

Talos 当时只返回"用户拒绝了这次调用",模型把同一条 `del verify_salary.py` 提了四遍。
**现在的修法和书完全一致** —— 拒绝里写明拒的是什么、为什么、该换什么动作。只是晚了一次事故。

### 3. 破坏性捷径

> **ch5:229** —— "这类**破坏性捷径总是存在**:即使把限制写进最终评估指标,Agent 也常能
> 找到绕过去的办法 —— 这正是第七章讨论的 reward hacking 在 Agent 任务中的日常形态。
> 因此生产级 Harness 要对 `rm -rf`、删除生产数据、覆盖未读文件这类危险动作设置专门的
> 检查与审批,约束的是**动作**而非仅仅是结果"

`FINDINGS.md` 里那句"每次加宽 `_DESTRUCTIVE` 都被路由过去",书当结论写。

---

## 二、书替我们说出来的那条主论点

十六个任务撞出来的结论是:**判断类规则不成立,形状类规则成立;把形状放进代码。**
书是当设计原则写的:

> **ch5:498** —— "三重保障由此完整:(1) 系统提示词的自然语言规则帮助理解和解释;
> (2) 工具描述与参数设计作为 checklist,引导模型在调用前显式核对条件;
> (3) 服务端基于数据库真值的**代码化校验作为最后守门员**。
> **前两重减少错误的发生,第三重确保错误不会变成不可逆的损失。**"

> **ch5:429** —— "代码化规则的真正价值不在于优化 token 效率,而在于**防止不可逆的错误
> 操作**...这种安全保障的价值远超其实现成本"

Talos 的 `write_file` 拒绝无 assert 的验证脚本、`_strip_workspace_prefix`、删除必须单独按 `y`
—— 全是第三重。而那些反复失败的 SYSTEM 条款,全是第一重被当成第三重用。

同样值得记下来的是书对 ch8 自学习的安全边界:

> "若允许正在运行的模型依据未经验证的反馈直接修改自身,**错误经验和提示注入就可能被固化**"
> "**证据与指令分离**,新知识不能作为指令直接执行"

这正是 `retrieve()` 里"memory.md 里像指令的行不注入 + ⚠️ 警告"那一段。**这条已经对了。**

---

## 三、改进清单(按优先级)

排序依据:收益确定性 × 改动大小,并且优先选符合那条铁律的
——**约束一个本来就要做的动作 ✅ / 要求新增一个动作 ❌**。

### P0 · 权限闸门:承认黑名单补不完 〔待定,需要先测〕

| | |
|---|---|
| **Talos 现在** | `_DESTRUCTIVE` 正则 + 已上线但**从没被真实运行触发过**的 `state["denied"]`(拒绝粘文件名)。三次事故三次加宽,三次被绕。正则永远看不见 `python -c "os.remove('x.md')"`、看不见一个会删文件的 `.py`、看不见 `move x nul`。 |
| **书里** | 黑名单"不应作为唯一手段"。两条升级路径:**语义解析**(ch4:192),或 **Sidecar 门控**(ch4:212–216)—— 一次独立的轻量 LLM 调用判断"这个工具调用是否安全",与主模型流式输出并行,亚秒级。关键设计:"**只看结构化的工具调用数据(工具名、参数),不看主模型的自由文本思考过程 —— 这是一个有意的设计,目的是防止主模型通过话术来操纵权限判断**"。兜底见 ch5:262:"权限分类连续失败就回退到人工询问"。 |
| **优势** | 判意图而非判写法,是唯一能终结"加宽→被绕"循环的方向。"只看结构化字段"这条 Talos 完全没有:现在的闸门看命令字符串,而模型可以在同一轮里用理由说服人按 `y`。 |

**暂不动,理由有二:** Sidecar 让每次 `run_bash` 多一次 LLM 调用,对这个极简项目是重量级
依赖;而书自己(ch5:262)说阈值该来自产线数据,`denied` 那个修复**一次都还没被真实跑触发过**。

**倾向的方向不是"判得更准",是"把不可逆变成可逆":** 跑任何未逐条批准的 `run_bash`
之前,把 `skills/`、`memory.md` 和本次请求点名的产出复制到 `.talos/trash/`。它不解析任何
东西,所以任何写法都绕不过去 —— `cmd /c`、`os.remove`、生成的 `.py` 一视同仁。
跟 ch5:114 的网络出口控制同源:**别去识别攻击,去掐断后果。**

### P1 · 写技能之前先查重 〔已做〕

| | |
|---|---|
| **Talos 现在** | `reflect()` 每任务写一条正式技能。`REFLECT_PROMPT` 只防**同名**覆盖,不防**同义**新增。十六个任务长出十二条,一半是噪声,靠 `/consolidate` 砍到六条。 |
| **书里** | ch8:"单次成功仅生成**候选**,需多条非失败轨迹支持才能升级为正式知识";"**文本记载时刻不等于学习时刻**,学习发生在评价、对照、归纳、验证之后"。ch3 给了可抄的机制:抽取候选 → 检索找相近的已有记忆 → LLM 在 **ADD / UPDATE / DELETE / NOOP** 中做决定。 |
| **优势** | 源头掐噪声,不靠 `/consolidate` 补救 —— 直接结掉 `FINDINGS.md` 里"噪声技能自动检测"那条挂账。书的答案不是**检测**,是**别写**。`NOOP` 是 Talos 完全没有的档位:现在只有"写"和"不写",没有"这条我已经会了"。 |

**做法:** 复盘前跑 `recall.explain(task)`,把命中的已有技能摆进 `REFLECT_PROMPT`,
把"新建"从默认动作变成一次四选一。零新工具、零新目录。
候选区 `skills/_candidates/` 是第二级梯子,这级不够再上。

### P2 · description 写成路由条件 〔已做〕

| | |
|---|---|
| **Talos 现在** | `description: 调试API响应结构,识别字段路径和响应格式,解决字段不存在错误` —— 纯功能介绍。正文有 `## 何时用`(`REFLECT_PROMPT` 强制的,这条是对的),但 description 没有。而 `recall.py` 拿 description + 正文的关键词跟任务句求交集 —— 功能介绍匹配的是"API/字段/响应",不是用户会怎么开口。 |
| **书里** | ch2:"description 字段是**路由决策**的关键...写法要像**路由条件而非功能介绍**。最直接的写法是 '**Use when / Don't use when**' 加上几条**反例**"。 |
| **理由** | ①反例和场景句里出现的是用户原话词,不是术语。②`不用于` 是**负向信号** —— 现在打分只有正向,"该扣住"全靠 `BODY_LEAD` 落差这个间接判据硬扛。 |

**实测结果:没有效果。而且我差点把没效果的东西当成大胜报出去。**

改完六条 description 之后,我编了 7 个查询句测,得到 top-1 命中 4/6 → 6/6、
正文注入 2/6 → 5/6 —— 看着像个大胜。**但那 7 个查询是我在写完 description 之后编的。**
换成 `.talos/sessions/` 里**用户自己打的 9 条真实任务句**重测:

| | 拿到正文 |
|---|---|
| 改前(功能介绍式) | 4/9 |
| 改后(路由条件式) | 4/9 —— **九条查询逐条输出完全一致** |

一个字都没变好。原因也清楚:那 5 条没命中的任务句,对应的技能在
`/consolidate` 时就被合并进 `memory.md` 了 —— **库里根本没有那条技能,描述写成什么样都捞不出来。**
这个语料的天花板就是 4/9。

所以这条改动的状态是:**理由成立,证据没有。** 保留(它不会变差,而且约束的是以后写的技能),
但 `FINDINGS.md` 里不许出现"提升了检索命中率"这句话。

顺带,这正好是 ch6 那段话的现场演示:

> "**评估集过拟合**:小评估集会导致系统设计去迎合具体任务而非泛化"
> "从**生产轨迹**中筛选...沉淀为评估集的新用例"

**自己写改动、又自己写评估集,等于自己给自己判卷。** 以后凡是要报检索质量的数字,
查询句一律从 `.talos/sessions/*.jsonl` 里取,不许现编。这条记进纪律。

### P3 · KV Cache:动态内容的位置 〔挂起,先测〕

| | |
|---|---|
| **Talos 现在** | `agent.py` 把 `learned`(memory.md 全量 + 技能描述)和 `recalled`(按 query 检索)拼进 system。 |
| **书里** | ch2 三条铁律的头两条:"**系统提示词和工具定义一旦确定就不要改。** 任何改动,哪怕多一个空格,都会导致缓存全部失效";"**动态信息永远追加到末尾**"。状态栏"在 API 层面实际上是作为**一条 user 角色的消息**插入到上下文末尾"。 |

**这条一开始被估成"3 行改动",是错的。** 单任务内 `system` 只算一次,`while` 循环里
步步命中缓存;把动态块挪到末尾,它就会**在任务内部每一步都被重算**。一个 20 步的任务:
挪过去要多算 20 × 约 1.5k token,省下的只是回合开始时的一次全量重读。**很可能是净亏。**

书那套只在动态块**只追加不重算**时成立 —— 每个回合追加一块新的,旧的留在历史里。
那是真重构,而且有真风险:`/consolidate` 删掉技能之后,历史里的旧描述还在。

**所以先测再说:** `state["tok"]` 已经在记 cached 数,先量出跨回合到底亏多少,
再决定值不值得改。**书是对的,但"照书改"这个动作的收益在这里没被验证过。**

### P4 · 检索质量回归网 〔大部分已存在〕

`tests/test_memory.py` 里已经有六条 recall 测试,包括 `test_a_past_task_at_rank_one_does_not_block_the_skill_body`
—— 上一轮那个 lead 规则修复**已经被网住了**。真正缺的只是"真实语料上的质量数字",
而那个依赖用户自己的 `skills/`,做不成可复现的离线测试。

书(ch6)在这件事上最该记住的三句:

> "单次运行的数字不应作为部署依据,而应**多次运行取均值**(每配置跑 3–5 次),同时报告均值与波动范围"
> "**分差小于噪声带宽时,不做切换决策**"
> "**每一轮只改变一个变量**"

`b18eb18` 那次一口气改了三处(`_DESTRUCTIVE`、`denied` 状态、`recall` 的 lead 规则),
真出回归无法归因。**这是流程问题,不是代码问题** —— 记在这里当纪律。

标准误 √(p(1−p)/n):n=16 的置信区间大到能吞掉几乎任何改动。所以 `FINDINGS.md` 里
所有"改前 X → 改后 Y"的数字,定位都只能是"**筛选值得扩大验证的方向**",不是结论。

### P5 · 拿"致命三要素"重写 SECURITY.md 〔待做〕

> **ch5:94**(引 Simon Willison)—— 访问私有数据 / 暴露于不可信内容 / 具备对外通信能力,
> **三者齐备即构成一条完整的攻击闭环**。
> **ch4:148** —— "**持久记忆会让攻击的影响跨会话持续,进一步放大风险**"

Talos 读本地文件 ✅、`run_bash` + 出网 ✅、读网页和工具输出 ✅ —— **三要素集齐,而且有
持久记忆。** `SECURITY.md` 现在是一张 bug 修复表,没有这个框架。

ch5:114 还给了一道 Talos 没有的闸:"**默认断网,按需通过白名单代理放行有限目的地**...
即使提示注入成功、恶意代码在沙盒内读到了敏感数据,没有出口就传不出去。相比试图识别
每一次注入,**掐断数据外传通道是确定性得多的防线**。"

### 观察项 · 技能正文超长

`log-analysis-tool.md` 是 9882 字节,而 `SKILL_BODY_MAX = 1200` —— 命中也只送前 1200 字。
书的三层渐进披露正好治这个:第二层 `SKILL.md` 只放核心流程,细则**放子文档由文件引用深入**。
但现在只有一条技能超标,而且这是**新增结构**。等第二条、第三条出现再动。

### 已否决 · frontmatter 加 `verified` 日期

ch8 说正式知识文档要有"最近验证时间"。查了一遍:`dead()` 里 `n.get("src", "user")` 对技能
恒为 `"user"`,**技能根本不参与老化判断**。先加一个没人读的日期字段等于零收益,
真要做得先让技能进 `dead()` 的视野。记在这里,别忘了它为什么没做。

---

## 四、明确不抄

| 书里的 | 为什么 |
|---|---|
| GraphRAG / 知识图谱 | 书自己说"三元组不可避免地导致**语义降级**"、"错误提取会导致**知识污染**",推荐的是"完整自然语言保存核心信息,辅以结构化元数据进行索引和检索"——**Talos 现在就是这个,已经对了** |
| Sidecar(现阶段) | 每次 `run_bash` 多一次 LLM 调用;而且 `denied` 修复还没被真实跑验证过,先测再说 |
| 五层上下文压缩 | 单层 `maybe_compact` 够用,书那套是给 367,000 字符溢出场景的 |
| 候选/正式**分库** + `release_manifest.json` + 回滚 | 生产系统规格,Talos 有 git |
| 每用户 LoRA / 后训练(ch7) | 不适用 |

---

## 五、最该记住的一句

> **ch8** —— "把对话存进记忆也不等于学会了新的行为:原始轨迹可能很长,其中既有有效策略,
> 也有**偶然成功、错误归因和不可信输入**。"

Talos 的自学习到今天为止,记的是"发生过什么",不是"验证过什么"。P1 是往那个方向迈的第一步。
