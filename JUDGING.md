# 怎么知道一条判据在不在判

绿灯有两种意思:**「这段代码是对的」**,和**「没有人在看这段代码」**。
它们长得一模一样。

这份文档讲怎么把这两种分开。内容全部来自一个真实项目里踩出来的坑 —— 一年多、
两百多条判据、每一条形状底下都有一次真实翻车。不提那个项目,因为这些形状跟它无关。

> **判据**(judgement)在这里指任何「不满足就该变红」的东西:一条断言、一条 lint 规则、
> 一次 CI 检查、一个 schema 校验、一个 code review checklist 里的条目。
> 「测试」这个词太窄 —— 下面六种形状里有四种发生在不叫 test 的东西上。

---

## 一、六种「判据本身没人查」

判据是用来查代码的。**而查判据的东西,通常一个都没有。**
下面六种全部是真实发生过的,不是设想:

| # | 形状 | 真实实例 |
|---|---|---|
| ① | **判据存在,但根本没被执行** | 800 行代码推上主干,CI 里一行都没跑到它们;补进去,第一次跑就抓到一个编码崩溃 |
| ② | **判据和被判对象共享盲点** | 一条正则要匹配全角标点,字符类里那两组「全角」码点其实是**半角写了两遍**;配套测试列了三种写法,**也全是半角**。谁也发现不了谁 |
| ③ | **判据结构上不可能红** | 一条测试用 `{"path": "a\u0000b.py"}` 验守卫对 NUL 路径的健壮性。JSON 解析当场就拒了,那个 NUL **从来没到过守卫**;断言一直靠上游被打桩才绿 |
| ④ | **样本太软,判据成摆设** | 排版检查器的自检样本只有 6 个块、4 层、短标签。把 9 个排版参数逐个拧到错值,判官 **8 个没反应** |
| ⑤ | **两条判据互相矛盾** | 一条要求某个量单调不增,另一条要求它在特定输入下增大。两条都绿,因为**没有任何机制会拿判据去比判据** |
| ⑥ | **从一个正确的例子,归纳出一个错误的全称** | 文档写「一旦拒绝过某个名字,**任何**提到它的东西都会再问一次」。它举的例子是对的,全称是假的 —— 换一种拼法就绕过去了 |

### 这六种的共同点

前两种最值得记,因为:**判据看起来在那儿,而且写判据的人相信它在那儿。**

第②种尤其难堪 —— 补测试时照着敲全角,**敲出来的又是半角**,和原作者一字不差的错误
犯了第二次,是打印码点才发现的。所以那里留了一句给下一个人:

> **改完打一遍码点,别信眼睛。**

还有一条必须说在前面:**知道有这六种,拦不住你再犯。** 上面这张表写完的第二天,
同一个人在同一个项目里同时犯了第③种和第④种。这份文档不是知识,是流程 ——
底下那件工具才是解法。

---

## 二、唯一通用的解法:反向验证

问题的本质是:**判据只在「东西坏了」的时候才有话说,而平时东西是好的。**
所以要主动把东西弄坏,看它说不说话。

> **摘掉这条判据要守的那个部件,判据必须变红。**
> 不是「改完是绿的」,是「退回旧实现时是红的」。

学名叫变异测试(mutation testing),但不必上框架 —— 一个五十行的脚本就够,
而且手写的那个你看得懂它在变异什么。

### 做法

```
对每一条要验的判据:
    1. 把整个项目复制到临时目录        ← 绝不在工作区上变异
    2. 在副本里,把守卫改成不守         ← 一次只改一处
    3. 跑测试
    4. 断言:那条判据出现在 FAILED 列表里
```

「把守卫改成不守」有几种常见写法:

- 把一个 `if` 条件反过来,或直接删掉那个分支
- 把一处 `problems.append(...)` 换成 `pass`
- 把一个常数拧到错值(行为藏在常数里、没有「规则」这种东西时用这招)
- 把两个分开的东西合并回去(验「拆开」这件事有没有意义时用这招)

### 三条纪律,每一条都是被坑出来的

**1. 锚点必须命中 —— 而且「命中」不等于「忠实」。**

连续三次「退回验证」误报绿灯,原因各不相同:

- 两次是变异**不忠实**:一个局部变量还指着旧字典;漏了重置那一行
- 一次是 `\b` 写在非 raw 的 Python 字符串里 —— 那是**退格符**,不是反斜杠加 b,
  于是替换从未命中,而脚本静默跳过了

所以脚本里一律:

```python
if old not in src:
    print("锚点没命中 —— 这条没验成,别当它绿")
    continue          # 计入失败,不计入通过
```

**一个不忠实的变异体报绿,比没验更糟** —— 它发的是假的安心。

**2. 在副本上做,不在工作区上做。** 变异脚本崩在中间是常事,
一个改坏的工作区比一条没验的判据危险得多。

**3. 报告「红了哪几条」,不只是「红了没有」。** 变异体让**别的**判据红了、
而目标那条照旧绿 —— 这在计数上是「有测试红了」,在意义上是没验成。

### 它能查出什么

第一次跑,在一个 21 条规则的检查器上,当场查出 **5 条摘掉之后自检照样绿**。
其中最难看的一条:上一轮审计发现了洞、**当场补了检查,但没补样本** ——
它随时可以原样烂回去,而没有人会知道。

### 它自己也会错

那个扫描脚本的第一版就是错的:`ast.walk` 是广度优先、`NodeTransformer` 是深度优先,
第 i 个变异和第 i 个标签**不是同一条规则**。数量对、名字全错。

暴露它的不是自查,是它跟一次外部审计**只重合两条**这个分歧。排序修好之后,
两种独立方法给出完全一致的五条。

**所以:验判据的工具,本身也需要被验。** 这个递归在实践中停在「两种独立方法给出同样的答案」。

---

## 三、判据不许因为环境缺东西而 skip

```python
mod = pytest.importorskip("some_optional_dep")   # ← 判据变成了形状①
```

装不上依赖的机器上,这条判据不是失败,是**消失**,而 CI 照样绿。

能静态查的就静态查。读源码(`ast`)通常比 import 更省事,而且更准 ——
拿正则去捞 `ui.` 开头的调用,会把注释里写的 `panel_ui.py` 数成一个叫 `py` 的函数;
`ast` 不会。

---

## 四、该枚举哪一侧

写守卫的时候总要划一条界。三种情况,三种划法:

| 情况 | 划法 | 为什么 |
|---|---|---|
| **非法的一侧是闭集** | 枚举非法的 | 枚举合法的永远落后一步 —— 下一个合法写法出现时,守卫拦错人 |
| **合法的一侧是闭集** | **枚举全部,要求每个都归档** | 见下 |
| **两侧都是开集** | **先把可达面锁小** | 见下 |

### 枚举全部,要求每个都归档

例:「主循环里不许自己 print」。

直接查「有没有 print」会误伤 —— 启动引导和 CLI 入口本来就该 print。
于是列一张**豁免名单**,每一项连理由一起写:

```python
_TALKS_DIRECTLY = {
    "_load_dotenv": "跑在 stdout 切成 UTF-8 之前,走界面就是循环依赖",
    "approve_tools": "CLI 子命令,主循环还没起来",
    ...
}
```

判据两头都查:

- 名单外出现 print → 红
- 名单里某项**不再对应任何代码** → 也红(留着一张空头支票,下一个撞上同名函数的人白拿通行证)

这条判据买的不是「拦住 print」,是**「拦住没人看的 print」** ——
新加一处,不管加在哪儿,都得先在表里写一行理由。

### 两侧都是开集时:先把可达面锁小

例:「这个只读模块不许写文件」。

写文件的写法是开集:`open(w)`、`pathlib.write_text`、`shutil.copy`、`os.replace`、
起一个子进程再写……禁止清单列不完。合法的一侧同样列不完。

出路是换一个层面:**它能 `import` 什么是闭集。**

```python
assert imported == {"collections", "json", "os", "tkinter", "tkinter.ttk"}
```

钉死这 5 个之后,里面没有 `pathlib`、没有 `shutil`、没有 `subprocess`,
写文件的通道就只剩 `open()` 和 `os.*` 两条 —— 而这两条各自也是闭集,逐个查得完。

加一个 import 就红,这正是要的:**新开一条通道,得有人看一眼它通向哪儿。**

---

## 五、两个更难的问题

### 谁来查判据

有一个部分答案,而且只有这一个:

> **换一个没参与写它的人(或模型)。**

自查为什么无效,第②种形状已经给过答案 —— 补测试的人照着敲,敲出的是同一个错误。
共享盲点是**结构性**的,不是态度问题。

实践中有效的组合:一次外部复查、一个不知道你意图的评审者、
或者一个跟你用不同方法得出同样结论的独立实现。

> 我写的判据,我当然觉得它在测该测的东西 ——
> 而它到底测没测,只有不知道我意图的人才看得出来。

### 谁来跑判据

这个问题更硬,而且**「写得便宜」会骗你**。

一条规则「不许在验证脚本里写死字面量断言」,做成检查只要二十行。
难的是:**谁来跑它?**

- 验证脚本自己查不了自己(自证)
- 叫人/模型「记得跑一下第三方检查」——**那还是一条提示,不是判据**

> **一条判据只有挂在「已经必跑的东西」上才算数。**

所以真正的设计问题是:流程里**已经必跑**的是哪一样,能不能把新检查挂上去。
挂不上去的话,你写的不是判据,是一条措辞更准的建议。

这跟上一节不是同一件事:上一节问谁来**查**判据,这节问谁来**跑**它。
**一条没人跑的判据,和一条写错的判据,在绿灯上长得一模一样。**

---

## 六、立完一道闸,先问这片面还有几条路

前五节讲的都是**写完判据之后**怎么查它。这一节讲**写判据之前**——
因为最贵的一类错发生在更早的地方:**判据本身是对的,只是被挂在了一条路上,
而那片面有三条路。**

真实的一天,同一个形状踩了三次:

| 想守住的那件事 | 挂了判据的通道 | 当时没人看的通道 |
|---|---|---|
| 凭据文件不许进上下文 | `read_file` 拒读 | 存快照的代码自带一套 guard,从不调用那道闸,照拷 |
| 同上 | 同上 | 子进程继承 `os.environ`,一句 `echo %KEY%` 就出来了 |
| 同上 | 同上 | shell 直接 `type .env` |
| 文档里可重算的数字不许烂 | 判据钉了 A 文档的行数 | B 文档里**同一个数字**,和 A 文档里**另一类**同样可重算的数字 |

每一次,判据都是对的、变异测试也红过、复查也点头。**它们只是各自看着一扇门,
而房间有四扇。** 而且注意最后一行:同一份文档里两类同样可重算的数字,只查了一类
——「另一条路」不一定在别的模块,常常就在隔壁那一行。

### 三个问题,写守卫之前问

1. **我要守的是「谁不能发生」,还是「哪个函数不能被调用」?**
   前者是**后果**,后者是**通道**。写成后者就一定会漏,因为通道是开集。
   凭据不许进上下文是后果;`read_file` 拒读只是一条通道。

2. **能不能挂在后果上?** 找**漏斗** —— 所有通道最后都得经过的那一个地方。
   有漏斗就挂漏斗:一处顶所有调用方,以后新开的通道自动被覆盖。
   (上面那张表的第 1–3 行,后来是靠「工具结果只有一个出口」一次性收掉的。)

3. **没有漏斗怎么办?** 那就回到第四节:**把可达面锁小**,让通道从开集变成闭集,
   再逐条归档。锁不小、也没有漏斗的,**在文档里写明它漏**——
   比留一句「已修」危险得多的,是留一句**看起来覆盖全片、实际只覆盖一条路**的「已修」。

### 一个反直觉的推论

**「这里已经有判据了」是最容易骗到自己的一句话。**
上面四行里,每一行的第二列都已经有判据、有事故记录、有变异测试。
正因为那道闸看起来是新修的、结实的,没人再问「它守的那件事还有别的入口吗」。

> 一道闸的强度,不取决于它自己有多结实,取决于**绕过它有多容易**。

---

## 一页 checklist

写完一条判据,问自己:

- [ ] **摘掉它要守的部件,它会红吗?** —— 别推测,真跑一遍(第二节)
- [ ] 变异体**忠实**吗?锚点命中了吗?红的是**目标那条**吗?
- [ ] 它在**每一个**目标环境里都会跑吗?有没有 `skipif` / `importorskip` 把它变没?
- [ ] 判据和被判对象是**同一个人在同一个下午**写的吗?那它们大概率共享盲点
- [ ] 样本够硬吗?把参数逐个拧到错值,它反应几个?
- [ ] 它跟别的判据**矛盾**吗?(没有任何机制会替你发现这件事)
- [ ] 它是从**一个例子**归纳出的**全称**吗?那个全称的边界在哪?
- [ ] 断言的一侧是**写死的字面量**吗?那它只是在自证 —— 数字照着改就过
- [ ] 流程里**已经必跑**的哪一样会执行它?
- [ ] **这道闸守的那件事,还有别的入口吗?**有漏斗就挂漏斗;没有就锁小可达面;两样都做不到,就在文档里写明它漏(第六节)

---

## 局限

样本是一个项目、一个人(加几次外部复查)、一年多。六种形状是**观察到的**,
不是穷举出来的 —— 第五种和第六种都是在「已经写完前四种」之后才撞见的,
所以大概率还有第七种。第六节更是**在写完前五节的同一天**撞出来的 —— 那一天连踩三次,而前五节一条都没拦住,因为它们查的是「判据对不对」,不是「判据够不够」。

反向验证这件事本身是老东西(变异测试有几十年历史)。这里唯一的主张是:
**它在「判据的正确性」这个问题上,是目前唯一能拿到证据的手段** ——
其余的做法(复查、结对、checklist)全都停在「有人觉得它对」。
