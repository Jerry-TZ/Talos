__pycache__/
*.py[cod]
.venv/
venv/
env/
.env
*.log
.DS_Store

# learned runtime state — un-ignore if you want to version the agent's knowledge
memory.md
skills/
tools/
.talos/

# default workspace created by talos.bat — the agent's output, not the project's
/workspace/

# the agent's scratch: debug scripts it writes mid-task, and generated output.
# Anchored to the repo root so tests/test_*.py stays tracked.
/debug_*.py
/check_*.py
/verify_*.py
/fix_*.py
/test_*.py
/*.docx
/*.xlsx
/crawl_out/

# IDE
.idea/
.vscode/
qr.png
人设/
